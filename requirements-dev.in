# Для тестов и CI. Закреплённый список с хешами — requirements-dev.txt:
#   uv pip compile requirements-dev.in --universal --python-version 3.12 --generate-hashes -o requirements-dev.txt
-r requirements.in
pytest>=9.0.3
ruff
pip-audit
